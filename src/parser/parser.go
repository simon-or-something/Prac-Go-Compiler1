package parser

// decl type stmt (homage to LLVM)
// i can do it with ImmediateEvaluation + RuntimeEvaluation too
// besides, this, or something similar, is more for LISPs (LISPs have something called "Special Forms")

type DeclNode struct {
	
}

type TypeNode struct {
	
}

type StmtNode struct {
	
}

type AstNode struct {
	
}
